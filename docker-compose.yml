services:
  booking-parser:
    build: .
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    environment:
      - PORT=${PORT:-8000}
    volumes:
      - ./output:/app/output
      - ./logs:/app/logs
    restart: unless-stopped
